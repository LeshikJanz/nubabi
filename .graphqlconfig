{
  "schemaPath": "graphql.schema.json",
  "includes": ["__generated__/*.graphql"],
  "extensions": {
    "endpoints": {
      "dev": {
        "url": "http://localhost:8080/graphql",
        "headers": {
          "Authorization": "${env:NUBABI_AUTH_TOKEN}"
        }
      },
      "firebase": {
        "url": "http://us-central1-nubabitest1.cloudfunctions.net/handler",
        "headers": {
          "Authorization": "${env:NUBABI_AUTH_TOKEN}"
         }
      },
      "aws": {
        "url": "https://79paqji348.execute-api.us-east-1.amazonaws.com/dev/graphql",
        "headers": {
          "Authorization": "${env:NUBABI_AUTH_TOKEN}"
         }
      }
    }
  }
}
