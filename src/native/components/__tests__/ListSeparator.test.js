import 'react-native';
import React from 'react';
import ListSeparator from '../ListSeparator';
import { expectRender } from '../../shared/testUtils';

test('it renders correctly', () => {
  expectRender(<ListSeparator />);
});
